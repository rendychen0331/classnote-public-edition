<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <application
        android:name=".ClassNoteApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClassNote">

        <!-- 全螢幕鬧鐘介面 -->
        <activity
            android:name=".ui.ReminderAlarmActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.ClassNote"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BroadcastReceiver for alarm notifications -->
        <receiver
            android:name=".notification.ReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.rendy.classnote.REMINDER_ALARM" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver for notification quick actions (延後 / 完成) -->
        <receiver
            android:name=".notification.ReminderActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.rendy.classnote.ACTION_SNOOZE" />
                <action android:name="com.rendy.classnote.ACTION_COMPLETE" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver for AI batch confirm / cancel -->
        <receiver
            android:name=".notification.AiConfirmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.rendy.classnote.AI_CONFIRM" />
                <action android:name="com.rendy.classnote.AI_CANCEL" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver for weather daily notification -->
        <receiver
            android:name=".notification.WeatherNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.rendy.classnote.WEATHER_NOTIFICATION" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver to reschedule alarms after reboot -->
        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- AppWidget -->
        <receiver
            android:name=".widget.ClassNoteWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.rendy.classnote.SWITCH_TAB" />
                <action android:name="com.rendy.classnote.PREV_MONTH" />
                <action android:name="com.rendy.classnote.NEXT_MONTH" />
                <action android:name="com.rendy.classnote.COMPLETE_REMINDER" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/appwidget_info" />
        </receiver>

        <service
            android:name=".widget.OverviewRemoteViewsService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <!-- 錄音 Foreground Service -->
        <service
            android:name=".ui.classrecord.AudioRecordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- 控制中心快捷鍵：助手模式懸浮覆層 -->
        <service
            android:name=".notification.QuickAddTileService"
            android:exported="true"
            android:icon="@drawable/ic_quick_add_tile"
            android:label="@string/tile_quick_add"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>

        <activity
            android:name=".notification.DismissShadeActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:exported="false" />

        <service
            android:name=".notification.FloatingQuickAddService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <meta-data
                android:name="android.app.FOREGROUND_SERVICE_SPECIAL_USE"
                android:value="overlay" />
        </service>

        <!-- AI 通知解析：監聽所有通知並用 Gemini 自動辨識作業/活動 -->
        <service
            android:name=".notification.ClassNoteNotificationListener"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- MSAL browser tab for Microsoft sign-in redirect -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="com.rendy.classnote"
                    android:path="@string/msal_redirect_path"
                    android:scheme="msauth" />
            </intent-filter>
        </activity>

        <!-- FileProvider for camera photos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager for background work -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge"
            xmlns:tools="http://schemas.android.com/tools">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>

</manifest>
